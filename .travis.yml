language: python
sudo: false
dist: trusty
cache:
  - pip
  - apt
  - ccache

addons:
  apt:
    packages: &common_packages
      - build-essential
      - gfortran
      - libatlas-dev
      - libatlas-base-dev
      - eatmydata
      - libhdf5-serial-dev
      - libpng12-dev
      - libfreetype6-dev
      - python-dev
      - libblas-dev
      - libc6
      - python-nose
      - hdf5-tools
      - pkg-config
      - g++
      - libx11-dev

python:
  - 2.7
  # - 3.5

env:
  - BARE_ENV=true
  # - BARE_ENV=false

before_install:
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a
  - if [[ "$BARE_ENV" = true ]]; then
      conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION numpy matplotlib cython;
    else
      conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION cython numpy matplotlib scipy numba h5py;
    fi
  - source activate test-environment

install: python setup.py install
script: python setup.py test
